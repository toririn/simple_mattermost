require 'spec_helper'

describe SimpleMattermost do
  it 'has a version number' do
    expect(SimpleMattermost::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
